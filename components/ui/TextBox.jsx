// const DOM = React.DOM;

// const TextBox = (props) => (
//   <span>{props.children}</span>
// )

// ReactDOM.render(
//   React.createElement(TextBox, {}, `Sometext`),
//   document.getElementById('app')
// );